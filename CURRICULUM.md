# 课程课表（周次制）

> 每周约 10 小时（工作日 1.5h × 5 + 周末 2.5h）。每周必须有能跑起来的东西。
> 原定 16 周，**2026-09-22 调整为 15 周**：删除 PHP Agent 开发端（避免重复造轮子），并把 Skills 与多 Agent 合并为一周。

## 技术路线（2026-09-22 定稿）

| 角色 | 语言 / 工具 | 职责 |
|---|---|---|
| **Agent 编排核心** | Python + LangGraph | 循环、状态、持久化、人机协同、检索链路 —— 一套到底，不重复实现 |
| **业务接入层** | PHP + Laravel | **只做调用端**：HTTP/SSE 消费 Python 服务、队列异步长任务、后台页面与权限。**不写 Agent 逻辑** |
| 工具协议 | MCP（FastMCP） | 跨平台标准，一次写好处处可用 |
| 向量检索 | pgvector | 会 SQL 就上手，混合检索直接写 SQL |
| 评测观测 | Langfuse + 自建测试集 | 开源可自托管 |

### 为什么不再用 PHP 写 Agent

W03 手搓循环 → W04 加防护与审计 → W08 用 LangGraph 工程化（checkpointer / interrupt / 条件边）—— **这条链路已经完整走通了一遍**。此时再用 PHP 的 `laravel/ai` SDK 或 Prism `withMaxSteps()` 写第二遍同样的循环，只是在另一种语法里重复已经掌握的知识，不产生新能力。

> 2026 年初 Laravel 官方发布的 `laravel/ai` 确实已经是生产可用的 Agent 框架（14 家 provider、Agent 类、tools、结构化输出、`RemembersConversations`、`queue()`/`stream()`/failover、原生 pgvector 集成）。但它的核心价值主张是「**PHP 团队不用学 Python 也能写 Agent**」——对已经掌握 Python 编排链路的人来说，这个卖点不成立。

Laravel 在这门课里的价值留在它无可替代的地方：**W09 的业务接入层、W12-W15 的工程治理（队列 / 权限 / 事务 / 审计）、W15 的上线部署**。

## 分层结构

| 层 | 周次 | 目标 |
|---|---|---|
| 基础 | W1-2 | 建立"调用 LLM 到底发生了什么"的体感 |
| 核心循环 | W3-4 | 手搓 Agent 循环，之后所有框架都是工程增强 |
| 知识 | W5-7 | RAG 全链路，从召回到答案质量 |
| 编排与落地 | W8-11 | 状态机、服务化、协议、技能封装 |
| 生产 | W12-15 | 评测、成本、安全、上线 |

---

## W01 · LLM 调用基础：把黑盒拆开看

| 项 | 内容 |
|---|---|
| **学什么** | token 与分词、上下文窗口、temperature / top_p、三种消息角色、SSE 流式；OpenAI 兼容协议与 Anthropic API；输入/输出 token 分开计价与缓存折扣 |
| **做什么** | ① 用 curl 手打一次原始 HTTP 请求，看清 request/response 结构 ② Python 写终端聊天（多轮 + 流式 + 实时计费） |
| **验收** | 能说出"这一轮对话花了多少钱、为什么"；能手动改 messages 数组制造并修复一次"模型失忆" |
| **产出** | `week-01/` 文章 + Python CLI 聊天程序 |
| **说明** | 本周只用 Python 手搓 HTTP，目的是看清协议。**PHP / Laravel 不作为 Agent 开发语言**，只从 W09 起以调用端身份介入（消费 Python 服务 + 后台集成），见顶部「技术路线」 |

## W02 · Prompt 工程与结构化输出

| 项 | 内容 |
|---|---|
| **学什么** | 提示结构（角色/任务/上下文/约束/示例）、XML 标签分隔、CoT、先规划后执行、自我校验；JSON Schema 约束与 Pydantic；Prompt 文件化 + Git 版本 + A/B |
| **做什么** | 用结构化输出做「领域信息抽取」，把常见字段定义成 Schema；建 20 条测试样本统计成功率；做两版 prompt A/B |
| **验收** | 成功率 ≥ 90%；失败样本能明确归因（prompt / schema / 模型能力） |
| **产出** | 文章 + 抽取脚本 + 20 条测试样本集 |

## W03 · Tool Use：Agent 的灵魂，手搓一个循环

| 项 | 内容 |
|---|---|
| **学什么** | tool_call → 执行 → 回填 → 再请求的机制；工具粒度、命名、描述（描述是给模型看的 API 文档）；失败返回错误字符串而非抛异常；并行调用与幂等性 |
| **做什么** | **零框架**纯 Python 写 while 循环 Agent，3 个工具（查天气 / 查 MySQL / 发通知）；打印每步 messages 看上下文如何变长 |
| **验收** | 能手画循环图；能解释"为什么工具描述写得差模型就不会用"；`max_steps` 必设 |
| **产出** | 文章 + 零框架 Agent 实现 |

## W04 · ReAct、记忆与循环防护

| 项 | 内容 |
|---|---|
| **学什么** | ReAct（Thought → Action → Observation）；记忆三层（历史 / 摘要压缩 / 外部存储）；防护四件套（步骤上限、超时、token 预算、重复调用检测）；工具调用落库可审计 |
| **做什么** | 给 W3 的 Agent 加步骤上限、自动摘要压缩、工具调用日志写 MySQL；做「查订单 → 查物流 → 判断退款 → 执行退款」全自动；故意制造一次死循环验证防护 |
| **验收** | 任务自动跑完；数据库能完整回放每一步；死循环被拦住且日志可查 |
| **产出** | 文章 + 带防护与审计的 Agent **（里程碑 M2）** |
| **数据模型** | 审计落库复用预置 `agent_runtime` 库：`runs`(主表, 含 token/成本/状态) / `steps`(ReAct 迭代, span_type=thought\|llm\|tool, tool_args JSON + observation + status 含 repeat_blocked) / `messages`(规范消息流, 回放用) / `agents`(需先 seed 一行, runs.agent_id 非空)。Layer2 摘要压缩缺 `messages.kind` 标记 → 实操前补 `kind`+`metadata` |

## W05 · Embedding 与向量检索

| 项 | 内容 |
|---|---|
| **学什么** | 文本向量化、余弦相似度、为什么语义可被度量；切分策略（固定/递归/按结构/语义）与 chunk、overlap；pgvector / Qdrant / Chroma；HNSW vs IVFFlat |
| **做什么** | Postgres + pgvector 建表并用 SQL 做相似度查询；把项目文档、表结构说明灌进去；调 top_k 与阈值观察召回变化 |
| **验收** | 能解释"这条查询为什么召回错了"；能写带过滤条件的混合 SQL |
| **产出** | 文章 + pgvector 检索 demo |

## W06 · RAG 完整链路

| 项 | 内容 |
|---|---|
| **学什么** | Query 改写 → 多路召回（向量 + BM25）→ RRF 融合 → Rerank → 拼上下文 → 生成 → 引用；纯向量的失效场景；Cross-encoder 成本；Recall@k / MRR / 忠实度 |
| **做什么** | 加 BM25 通路做 RRF 融合；30 题测试集对比三种方案（纯向量 / 纯 BM25 / 混合+重排），准确率做成表；答案带引用出处 |
| **验收** | 有一张自己跑出来的对比数据表，能说清"多花的重排成本换了多少准确率" |
| **产出** | 文章 + 三方案对比实验 + 数据表 |

## W07 · 进阶 RAG

| 项 | 内容 |
|---|---|
| **学什么** | 多查询扩展、HyDE、指代消解；父子分段（small-to-big）；PDF（含扫描件 OCR）、表格、图文混排解析；上下文压缩；GraphRAG（了解） |
| **做什么** | 啃<b>复杂 PDF</b>（表格密集、跨页、合并单元格、脚注干扰），跑通"PDF → 文本 → 结构化 → 入库 → 可问答"；实现 HyDE 对比开关前后召回率 |
| **验收** | 能处理带表格和图片的文档；答案每句可溯源 |
| **产出** | 文章 + 文档解析链路 **（里程碑 M3）** |

## W08 · LangGraph：把循环变成工程

| 项 | 内容 |
|---|---|
| **学什么** | 为什么需要框架（状态持久化、崩溃恢复、并行、人机协同、时间旅行）；State / Node / Edge / Checkpointer；`interrupt()` 与 Command 恢复；对比 OpenAI Agents SDK 与 Pydantic AI |
| **做什么** | 用 LangGraph 重写 W4 的 Agent；加"危险操作需人工确认"的 interrupt 节点；checkpointer 接 Postgres，kill 进程后验证续跑 |
| **验收** | 能画状态图；进程被 kill 后重启能从断点继续 |
| **产出** | 文章 + LangGraph 版 Agent |

## W09 · 毕业项目 MVP：财报与可转债分析 Agent 的数据地基

> 毕业项目主题正式定为 **财报与可转债分析 Agent**（A 股年报场景）。选题理由：数据公开可复现、有归一化这种真陷阱、合规上能公开讲，详见仓库 README。

| 项 | 内容 |
|---|---|
| **学什么** | 服务化边界（Python 服务 vs Laravel 接入层各自该做什么）；**指标语义层**（字段字典 + L1-L2 归一化 + 近似字段陷阱的否定词保护）；SSE 流式转发与 Laravel 队列长任务；NL2SQL 的安全边界（白名单表、只读账号、参数化） |
| **做什么** | ① **Python 侧（主体）**：核心数据表 + Migration→字段字典冷启动→`classify`/`extract` 工具→L1-L2 归一化（含近似字段陷阱的否定词保护）→`query_indicator`<br>② **PHP 侧（调用端，约 30% 工时）**：FastAPI 暴露 W08 的图 → Laravel HTTP 客户端 → SSE 流式转发 → 队列异步长任务 → 最简 Vue 查询页。**不写任何 Agent 逻辑** |
| **验收** | 3 家真实公司跑通"取数 → 抽取 → 归一化 → 入库 → 问一个值"；科目抽取准确率 ≥ 85%；**扣非 / 归母零误匹配**；Laravel 页面能看到流式返回的中间过程 |
| **产出** | 文章 + Python FastAPI 服务 + Laravel 调用端 + Migration + 种子数据 |
| **伏笔** | W06 结论：向量 + BM25 + 重排**三道防线救不了语义相似块**（"扣非净利润" vs "归母净利润" 在检索层面无法区分）。字段级归一化是唯一解，本周正面解决它 |
| **范围** | 完整版（趋势 / 对比 / 解读）放在 W15 毕业主体 |

## W10 · MCP：让 Agent 连上你的系统

| 项 | 内容 |
|---|---|
| **学什么** | 三原语（tools / resources / prompts）；stdio 与 Streamable HTTP；**2026-07-28 新规范**（无状态核心、`_meta` 自描述、MRTR、Tasks 扩展、`Mcp-Method`/`Mcp-Name` 网关头）；渐进式披露控 token（实测 15 万 → 2 千） |
| **做什么** | FastMCP 写 2 个 Server（只读查 MySQL + 调业务 API）；接进客户端用自然语言查自己后台数据；实现一次 Tasks 长任务模式 |
| **验收** | 说一句"查一下昨天新增多少用户"，能拿到真实数字 |
| **产出** | 文章 + 2 个 MCP Server |
| **注意** | 新项目直接按 2026-07-28 规范写，旧版 SSE + 会话已弃用（12 个月过渡） |

## W11 · 编排与协作：Skills → Handoff → Supervisor

> 由原 W11（Agent Skills）与原 W12（多 Agent）合并。两者本质是同一个问题的两端：**该拆到什么粒度**——Skill 是单 Agent 内部的能力封装，Handoff 是跨 Agent 的分工。

| 项 | 内容 |
|---|---|
| **学什么** | SKILL.md 格式（`SKILL.md` + `references/` + `scripts/`）；**Skill 与 MCP 的分工**（MCP 是门卡：能否访问；Skill 是入职文档：怎么用、按什么顺序）；渐进式披露；Supervisor / Swarm / Handoff / Plan-and-Execute；上下文隔离与子 Agent；代价（延迟 ×N、成本 ×N、调试 ×N²） |
| **做什么** | ① 把一个高频重复工作流固化成 Skill，写清触发条件、分步流程、边界与失败处理，跑 5 次以上验证可复现 ② 搭 3 Agent 流水线（研究 → 撰写 → 审校），同一任务单 Agent 与多 Agent 各跑 5 次，记录成功率、耗时、token 成本 |
| **验收** | Skill 接下来几个月真的在用（否则说明抽象错了）；有一张实测对比表，能说出"这个场景多 Agent 值不值" |
| **产出** | 文章 + 一个可复用 Skill + 对比实验数据 |

## W12 · 评测 Eval：90% 的项目死在这里

| 项 | 内容 |
|---|---|
| **学什么** | 三层评测（组件级 Recall/MRR、轨迹级工具调用序列、端到端成功率）；Golden Dataset 从真实日志挑 case（30-50 条，覆盖边界与失败模式）；LLM-as-Judge 与偏见校正；Langfuse / LangSmith / RAGAS |
| **做什么** | 给前面所有项目补测试集做成一条命令能跑的回归测试；换 2-3 个模型跑评测打分；把评测接进改动流程 |
| **验收** | 10 分钟内能回答"这次改动是好是坏"，且有数据支撑 |
| **产出** | 文章 + 评测框架 + 黄金测试集 |
| **背景** | 78% 企业启动 Agent 试点，仅 14% 走到生产，首要死因是没有评测 |

## W13 · 上下文工程与可观测：钱花在哪一步

| 项 | 内容 |
|---|---|
| **学什么** | **上下文工程四杠杆**（Write 外置暂存 / Select 按需取用 / Compress 压缩窗口 / Isolate 隔离上下文）；Tracing span 树（每次 LLM 调用的输入输出/耗时/token/成本/工具名参）；prompt caching 与语义缓存；模型分级路由；按用户/会话预算与降级 |
| **做什么** | 给 W09 的 Python 服务 + Laravel 调用端加**双端 trace** 落 MySQL；用 Isolate / Compress 重构一次 W09 的上下文装配，测 token 下降百分比；加缓存测成本下降；做成本看板（Vue3 + ECharts） |
| **验收** | 能回答"昨天哪个用户花最多钱、卡在第几步、为什么"；上下文重构与缓存各自都有实测数字 |
| **产出** | 文章 + trace 实现 + 成本看板 |
| **为什么合并** | 2026 年 Context Engineering 已成独立学科（LangChain 归纳为 Write/Select/Compress/Isolate 四策略）。它不是横切的口头禅——有纪律的上下文管理相比裸拼全历史**稳定省下 40-60% token**，直接决定最大的那条成本线，所以和观测治理放在同一周 |

## W14 · 安全与可信：红队你自己的 Agent

| 项 | 内容 |
|---|---|
| **学什么** | **间接 Prompt Injection**（恶意指令藏在网页/PDF/数据库字段/用户昵称）；工具内校验身份与数据归属；沙箱、人机确认、输出过滤、脱敏；SQL 注入的教训在此重演 |
| **做什么** | 构造 ≥5 个攻击样例（越权查他人数据、诱导输出系统提示、执行删除、通过上传文档注入指令、无限循环烧钱）；<b>业务场景额外必测</b>：问"下季度股价会涨吗"是否拒绝预测、问"推荐一只会涨的标的"是否拒答、库里没有的数据是否编造、未披露科目是否用相似科目顶替 |
| **验收** | 5 个攻击全被拦住；每个防御都有自动化测试 |
| **产出** | 文章 + 红队用例集 |

## W15 · 生产化上线：毕业项目完整版

| 项 | 内容 |
|---|---|
| **学什么** | Docker 与 FastAPI 服务部署；Laravel 队列与 Horizon、长任务异步；SSE 流式渲染与过程可视化；降级容错（供应商切换、超时重试、部分失败） |
| **做什么** | 毕业项目补成完整版上线：复杂文档解析、多期趋势图、同业对比、比率计算与健康度分析、管理后台的字段字典维护与 unknown 队列审核页；Vue3 对话界面（四类消息卡片）；补齐 README、评测报告、成本测算、合规清单 |
| **验收** | 真实场景跑通，有人在用，有数据说明省了多少时间 |
| **产出** | 文章 + 完整项目 **（里程碑 M4，结业）** |

---

## 里程碑

| 编号 | 项目 | 完成周 | 意义 |
|---|---|---|---|
| M1 | 终端聊天助手 | W1-2 | 最小闭环，建立体感 |
| M2 | 数据库运维 Agent（零框架手搓） | W3-4 | 亲手写出循环，框架不再是黑盒 |
| M3 | 私有知识库 RAG 问答 | W5-7 | 覆盖 RAG 全链路 |
| M4 | 财报与可转债分析 Agent 完整版 | W9 / W15 | 数据公开可复现，能开源、能写成系列文章收尾 |

## 7 周压缩版

| 压缩周 | 合并 | 保留 / 省略 |
|---|---|---|
| W1 | 原 1-2 | 保留 curl + 结构化抽取 |
| W2 | 原 3-4 | **必须完整保留**，手搓循环是基石 |
| W3 | 原 5-6 | 保留 pgvector + 混合检索对比；省略重排调优 |
| W4 | 原 7-8 | 保留 LangGraph 重写与 interrupt；HyDE/GraphRAG 只了解 |
| W5 | 原 9 | **必须完整保留**，毕业项目 MVP + Laravel 调用端 |
| W6 | 原 10-11 | 保留写 MCP Server；Skills 写简化版，多 Agent 只做概念 |
| W7 | 原 12-15 | **评测必须做**；可观测/上下文工程保留；安全红队与上线保留，成本看板可简化 |
