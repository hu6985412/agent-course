# 课程课表（周次制）

> 每周约 10 小时（工作日 1.5h × 5 + 周末 2.5h）。每周必须有能跑起来的东西。
> 以下 16 周为**初始规划框架**，实际周次与时长会随课程推进动态调整，逐步追加新周内容。

## 分层结构

| 层 | 周次 | 目标 |
|---|---|---|
| 基础 | W1-2 | 建立"调用 LLM 到底发生了什么"的体感 |
| 核心循环 | W3-4 | 手搓 Agent 循环，之后所有框架都是工程增强 |
| 知识 | W5-7 | RAG 全链路，从召回到答案质量 |
| 编排 | W8-11 | 状态机、协议、技能封装 |
| 生产 | W12-16 | 评测、成本、安全、上线 |

---

## W01 · LLM 调用基础：把黑盒拆开看

| 项 | 内容 |
|---|---|
| **学什么** | token 与分词、上下文窗口、temperature / top_p、三种消息角色、SSE 流式；OpenAI 兼容协议与 Anthropic API；输入/输出 token 分开计价与缓存折扣 |
| **做什么** | ① 用 curl 手打一次原始 HTTP 请求，看清 request/response 结构 ② Python 写终端聊天（多轮 + 流式）③ PHP 用 Prism 写同样功能做对比 |
| **验收** | 能说出"这一轮对话花了多少钱、为什么"；能手动改 messages 数组制造并修复一次"模型失忆" |
| **产出** | `week-01/` 文章 + CLI 聊天程序（Python & PHP 双版本） |

## W02 · Prompt 工程与结构化输出

| 项 | 内容 |
|---|---|
| **学什么** | 提示结构（角色/任务/上下文/约束/示例）、XML 标签分隔、CoT、先规划后执行、自我校验；JSON Schema 约束与 Pydantic；Prompt 文件化 + Git 版本 + A/B |
| **做什么** | 用结构化输出做「财报科目信息抽取」，把三大报表常见科目定义成 Schema；建 20 条测试样本统计成功率；做两版 prompt A/B |
| **验收** | 成功率 ≥ 90%；失败样本能明确归因（prompt / schema / 模型能力） |
| **产出** | 文章 + 抽取脚本 + 20 条测试样本集 |

## W03 · Tool Use：Agent 的灵魂，手搓一个循环

| 项 | 内容 |
|---|---|
| **学什么** | tool_call → 执行 → 回填 → 再请求的机制；工具粒度、命名、描述（描述是给模型看的 API 文档）；失败返回错误字符串而非抛异常；并行调用与幂等性 |
| **做什么** | **零框架**纯 Python 写 while 循环 Agent，3 个工具（查天气 / 查 MySQL / 发通知）；打印每步 messages 看上下文如何变长；再用 PHP + Prism `withMaxSteps()` 重写 |
| **验收** | 能手画循环图；能解释"为什么工具描述写得差模型就不会用"；`max_steps` 必设 |
| **产出** | 文章 + 零框架 Agent 实现（双语言） |

## W04 · ReAct、记忆与循环防护

| 项 | 内容 |
|---|---|
| **学什么** | ReAct（Thought → Action → Observation）；记忆三层（历史 / 摘要压缩 / 外部存储）；防护四件套（步骤上限、超时、token 预算、重复调用检测）；工具调用落库可审计 |
| **做什么** | 给 W3 的 Agent 加步骤上限、自动摘要压缩、工具调用日志写 MySQL；做「查订单 → 查物流 → 判断退款 → 执行退款」全自动；故意制造一次死循环验证防护 |
| **验收** | 任务自动跑完；数据库能完整回放每一步；死循环被拦住且日志可查 |
| **产出** | 文章 + 带防护与审计的 Agent **（里程碑 M2）** |

## W05 · Embedding 与向量检索

| 项 | 内容 |
|---|---|
| **学什么** | 文本向量化、余弦相似度、为什么语义可被度量；切分策略（固定/递归/按结构/语义）与 chunk、overlap；pgvector / Qdrant / Chroma；HNSW vs IVFFlat |
| **做什么** | Postgres + pgvector 建表并用 SQL 做相似度查询；把项目文档、表结构说明灌进去；调 top_k 与阈值观察召回变化 |
| **验收** | 能解释"这条查询为什么召回错了"；能写带过滤条件的混合 SQL |
| **产出** | 文章 + pgvector 检索 demo |

## W06 · RAG 完整链路

| 项 | 内容 |
|---|---|
| **学什么** | Query 改写 → 多路召回（向量 + BM25）→ RRF 融合 → Rerank → 拼上下文 → 生成 → 引用；纯向量的失效场景；Cross-encoder 成本；Recall@k / MRR / 忠实度 |
| **做什么** | 加 BM25 通路做 RRF 融合；30 题测试集对比三种方案（纯向量 / 纯 BM25 / 混合+重排），准确率做成表；答案带引用出处 |
| **验收** | 有一张自己跑出来的对比数据表，能说清"多花的重排成本换了多少准确率" |
| **产出** | 文章 + 三方案对比实验 + 数据表 |

## W07 · 进阶 RAG

| 项 | 内容 |
|---|---|
| **学什么** | 多查询扩展、HyDE、指代消解；父子分段（small-to-big）；PDF（含扫描件 OCR）、表格、图文混排解析；上下文压缩；GraphRAG（了解） |
| **做什么** | 啃<b>财报 PDF</b>（表格密集、跨页、合并单元格、脚注干扰），跑通"PDF → 文本 → 结构化 → 入库 → 可问答"；实现 HyDE 对比开关前后召回率 |
| **验收** | 能处理带表格和图片的文档；答案每句可溯源 |
| **产出** | 文章 + 文档解析链路 **（里程碑 M3）** |

## W08 · LangGraph：把循环变成工程

| 项 | 内容 |
|---|---|
| **学什么** | 为什么需要框架（状态持久化、崩溃恢复、并行、人机协同、时间旅行）；State / Node / Edge / Checkpointer；`interrupt()` 与 Command 恢复；对比 OpenAI Agents SDK 与 Pydantic AI |
| **做什么** | 用 LangGraph 重写 W4 的 Agent；加"危险操作需人工确认"的 interrupt 节点；checkpointer 接 Postgres，kill 进程后验证续跑 |
| **验收** | 能画状态图；进程被 kill 后重启能从断点继续 |
| **产出** | 文章 + LangGraph 版 Agent |

## W09 · PHP 落地：把 Agent 装进 Laravel 后台

| 项 | 内容 |
|---|---|
| **学什么** | `laravel/ai` 官方 SDK（Agent 类、tools、结构化输出、`RemembersConversations`、`queue()`、`stream()`、failover、`fake()` 测试）；Prism PHP `withMaxSteps()`；Neuron AI 能力边界；**工具内做鉴权** |
| **做什么** | 财报结构化 Agent **MVP**：建 4 张表（`fin_companies` / `fin_reports` / `fin_indicators` / `fin_report_items`）+ 科目字典冷启动 → akshare 取数（跳过解析）→ classify/extract 工具 → L1-L2 归一化（含<b>扣非陷阱</b>的否定词保护）+ save_report → 搭 Agent（queue 异步）→ `query_indicator` + 最简查询页 |
| **验收** | 3 家真实公司跑通"取数 → 抽取 → 归一化 → 入库 → 问一个值"；科目抽取准确率 ≥ 85%；<b>扣非 / 归母零误匹配</b> |
| **产出** | 文章 + Laravel Migration + Agent + 种子数据 |
| **范围** | 完整版（趋势/对比/解读）体量 40-60h，作为 W16 毕业项目主体 |

## W10 · MCP：让 Agent 连上你的系统

| 项 | 内容 |
|---|---|
| **学什么** | 三原语（tools / resources / prompts）；stdio 与 Streamable HTTP；**2026-07-28 新规范**（无状态核心、`_meta` 自描述、MRTR、Tasks 扩展、`Mcp-Method`/`Mcp-Name` 网关头）；渐进式披露控 token（实测 15 万 → 2 千） |
| **做什么** | FastMCP 写 2 个 Server（只读查 MySQL + 调业务 API）；接进 WorkBuddy 客户端用自然语言查自己后台数据；实现一次 Tasks 长任务模式 |
| **验收** | 说一句"查一下昨天新增多少用户"，能拿到真实数字 |
| **产出** | 文章 + 2 个 MCP Server |
| **注意** | 新项目直接按 2026-07-28 规范写，旧版 SSE + 会话已弃用（12 个月过渡） |

## W11 · Agent Skills：给 Agent 写入职文档

| 项 | 内容 |
|---|---|
| **学什么** | SKILL.md 格式（`SKILL.md` + `references/` + `scripts/`）；**与 MCP 的分工**：MCP 是门卡（能否访问），Skill 是入职文档（怎么用、按什么顺序）；渐进式披露；开放标准跨平台 |
| **做什么** | 把一个高频重复工作流固化成 Skill（如"从 Laravel Model 生成动态页面配置"）；写清触发条件、分步流程、边界与失败处理；跑 5 次以上验证可复现 |
| **验收** | 这个 Skill 你接下来几个月真的在用，否则说明抽象错了 |
| **产出** | 文章 + 一个可复用 Skill |

## W12 · 多 Agent：先学会什么时候不该用

| 项 | 内容 |
|---|---|
| **学什么** | Supervisor / Swarm / Handoff / Plan-and-Execute；上下文隔离与子 Agent；代价（延迟 ×N、成本 ×N、调试 ×N²） |
| **做什么** | 搭 3 Agent 流水线（研究 → 撰写 → 审校）；同一任务单 Agent 与多 Agent 各跑 5 次，记录成功率、耗时、token 成本 |
| **验收** | 有一张实测对比表，能说出"这个场景多 Agent 值不值" |
| **产出** | 文章 + 对比实验数据 |

## W13 · 评测 Eval：90% 的项目死在这里

| 项 | 内容 |
|---|---|
| **学什么** | 三层评测（组件级 Recall/MRR、轨迹级工具调用序列、端到端成功率）；Golden Dataset 从真实日志挑 case（30-50 条，覆盖边界与失败模式）；LLM-as-Judge 与偏见校正；Langfuse / LangSmith / RAGAS |
| **做什么** | 给前面所有项目补测试集做成一条命令能跑的回归测试；换 2-3 个模型跑评测打分；把评测接进改动流程 |
| **验收** | 10 分钟内能回答"这次改动是好是坏"，且有数据支撑 |
| **产出** | 文章 + 评测框架 + 黄金测试集 |
| **背景** | 78% 企业启动 Agent 试点，仅 14% 走到生产，首要死因是没有评测 |

## W14 · 可观测与成本治理

| 项 | 内容 |
|---|---|
| **学什么** | Tracing span 树（记录每次 LLM 调用的输入输出/耗时/token/成本/工具名参）；prompt caching 与语义缓存；模型分级路由；按用户/会话预算与降级 |
| **做什么** | 给 W9 的 PHP Agent 加全链路 trace 落 MySQL；加缓存测成本下降百分比；做成本看板（Vue3 + ECharts） |
| **验收** | 能回答"昨天哪个用户花最多钱、卡在第几步、为什么"；成本下降有实测数字 |
| **产出** | 文章 + trace 实现 + 成本看板 |

## W15 · 安全与可信：红队你自己的 Agent

| 项 | 内容 |
|---|---|
| **学什么** | **间接 Prompt Injection**（恶意指令藏在网页/PDF/数据库字段/用户昵称）；工具内校验身份与数据归属；沙箱、人机确认、输出过滤、脱敏；SQL 注入的教训在此重演 |
| **做什么** | 构造 ≥5 个攻击样例（越权查他人数据、诱导输出系统提示、执行删除、通过上传文档注入指令、无限循环烧钱）；<b>财报场景额外必测</b>：问"下季度股价会涨吗"是否拒绝预测、问"推荐一只会涨的转债"是否拒答、库里没有的数据是否编造、未披露科目是否用相似科目顶替 |
| **验收** | 5 个攻击全被拦住；每个防御都有自动化测试 |
| **产出** | 文章 + 红队用例集 |

## W16 · 生产化上线：毕业项目

| 项 | 内容 |
|---|---|
| **学什么** | Docker、FastAPI 服务化、Laravel 队列与 Horizon、长任务异步；SSE/WebSocket 流式渲染与过程可视化；降级容错（供应商切换、超时重试、部分失败） |
| **做什么** | 财报 Agent 补成完整版上线：财报 PDF 解析、5 年 20 期趋势图、同业对比、`calc_ratios` 财务比率与四维度健康度分析、可转债模块（溢价率/YTM/强赎下修条款）、管理后台的科目字典维护与 unknown 队列审核页；Vue3 对话界面（四类消息卡片）；补齐 README、评测报告、成本测算、合规清单 |
| **验收** | 真实场景跑通，有人在用，有数据说明省了多少时间 |
| **产出** | 文章 + 完整项目 **（里程碑 M4，结业）** |

---

## 里程碑

| 编号 | 项目 | 完成周 | 意义 |
|---|---|---|---|
| M1 | 终端聊天助手（Python + PHP 双版本） | W1-2 | 最小闭环，建立体感 |
| M2 | 数据库运维 Agent（零框架手搓） | W3-4 | 亲手写出循环，框架不再是黑盒 |
| M3 | 私有知识库 RAG 问答 | W5-7 | 覆盖 RAG 全链路 |
| M4 | 财报与可转债分析 Agent 上线 | W9 / W16 | 数据公开可复现，能开源、能写成系列文章收尾 |

## 8 周压缩版

| 压缩周 | 合并 | 保留 / 省略 |
|---|---|---|
| W1 | 原 1-2 | 保留 curl + 结构化抽取；省略双语言对比 |
| W2 | 原 3-4 | **必须完整保留**，手搓循环是基石 |
| W3 | 原 5-6 | 保留 pgvector + 混合检索对比；省略重排调优 |
| W4 | 原 7-8 | 保留 LangGraph 重写与 interrupt；HyDE/GraphRAG 只了解 |
| W5 | 原 9 | **必须完整保留**，差异化价值所在 |
| W6 | 原 10-11 | 保留写 MCP Server；Skills 写简化版 |
| W7 | 原 12-13 | 多 Agent 可跳过，**评测必须做** |
| W8 | 原 14-16 | 保留安全红队和上线；成本看板可简化 |
